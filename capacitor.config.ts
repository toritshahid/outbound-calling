import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'outbound-calling',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
